import React from 'react';

// The context is initialized with a default value.
// This value will be used if a component doesn't have a matching Provider above it in the tree.
export const LookupContext = React.createContext(null);
